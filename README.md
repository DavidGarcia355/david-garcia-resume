# David Garcia — résumé website

A concise portfolio in a one-page résumé layout, built with plain HTML and CSS.

## Local preview

Open `index.html` in a browser, or serve this folder with `python -m http.server 8000`.

The downloadable résumé is `resume.pdf`. Update that file when your CV changes.
